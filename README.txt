Goal: Create a simple typing tester that displays a paragraph of content, and allows the user to type the content out.

When the user's input doesn't match the paragraph, the text will go red, and the user will have to fix the errors.

A 1 minute timer will countdown, once it reaches zero, WPM will be displayed and text field will be blocked. 

User can hit retry button to restart application, and can select from multiple paragraphs? and multiple times(30s/60s)